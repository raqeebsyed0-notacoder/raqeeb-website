# Portfolio Audit — 2026-09-25 (AUDIT ONLY, no fixes applied)

Target: `E:/raqeeb-website` @ `4b0116c`, built + served at `http://localhost:4173` (hash routes).
Method: headless-Chrome screenshots (34 pages) + vision review, pixel-stat file analysis,
curl checks (routes, assets, outbound live URLs), console-error sweep, code grep.
`browser_exec` is session-blocked; rendering via headless CLI is the proven substitute.

## Q: white pages below website?
Pixel-measured blank export files (stddev 0.0 = zero content), blank at source — not a code bug:

| file | size | pixels |
|---|---|---|
| `grc-v1@1400w.jpg`, `grc-v1@1920w.jpg` | 1400×9000 | uniform near-white (mean 243) |
| `grc-v2@1400w.jpg`, `grc-v2@1920w.jpg` | 1400×9000 | uniform near-white (mean 241) |
| `grc-v3@1400w.jpg`, `grc-v3@1920w.jpg` | 1400×9000 | uniform near-black (mean 12) |
| `euphoria-experiences@1400w.jpg` + `@1920w` | 1400×9000 | near-white, faint header (std 24) |
| `grc-evolution-sheet@1400w.jpg` | 1400×241 | REAL content (control sample) |

`@1400w` and `@1920w` variants are byte-identical dimensions (and euphoria pair near-identical
pixels) — the "1920w" files were never rendered at 1920. Need designer re-exports.

## CRITICAL
1. Blank GRC gallery (6 files above) — GRC detail page shows 3 white cards + black card.
2. Blank euphoria gallery (2 files above) — euphoria detail page shows 2 white cards.

## HIGH
3. Marsa/Najd `@1400w`/`@1920w` are 381×1500 / 292×1500 mobile captures, misnamed as desktop widths.
4. Marsa live site preloader never dismisses under headless Chrome (loader at 75% even with
   30s virtual budget) — real-browser behavior UNVERIFIED; card currently uses static hero.

## MEDIUM
5. Mixed-ratio image grid (`ProjectDetailPage.tsx:55-64`, `object-contain`, no fixed row height):
   worst on editorial-posters (1280×268 pano renders as thin strip + black void; square poster
   letterboxed), visible black gaps beside hero images on marsa/najd pages.
6. About page: no real photo (RH monogram placeholder) + large empty void below principles.
7. Home hero video area renders black in headless captures; file itself curls 200 OK —
   likely headless-autoplay artifact, needs one real-browser confirm.

## LOW
8. Stale build files committed in `public/assets/` (`index-1ltLpFT7.js`, `index-DCDDTere.css`)
   get copied into `dist/assets` alongside current bundle.
9. `index.html` favicon points to `./icon.svg` which does not exist (`favicon.svg` does).
10. `portfolio.ts` sourceFolder escaping inconsistent (blaize 8 backslashes vs 4 vs 2) — metadata only.
11. `HTMLEmbedViewer` component still shipped but zero imports (dead code).
12. Generous blank space above footer on all detail pages (page-end padding).
13. "Organic B" client tag looks clipped on About (single vision claim, unverified).

## VERIFIED CLEAN
- All 34 routes serve 200 (5 static, 1 hub, 4 categories, 24 details — 25th is `grc`, counted).
- All 228 unique `assets/` references exist on disk; 26/26 sampled serve 200.
- Zero console errors/warnings on 7-route sweep (only benign Windows registry noise).
- PDF deck viewer (buyology 13pp, flowserve 39pp): first slide + counter + prev/next all render.
- All 4 carousels: correct height, slides loaded, dots + arrows present (websites shows 4 live fronts).
- Live card + "Visit live site" strip on all 4 website pages; GRC v1/v2/v3 buttons present.
- Outbound live URLs 5/5 curl-200 with correct titles (re-verified this session).
- Prev/next links correct (buyology→euphoria, flowserve↔chashmawala/jalal, arata→dot&key).
- Nav/footer links, VIEW RESUME (`/#/resume`), GET IN TOUCH + Let's talk (mailto) all wired.
- No lorem ipsum anywhere; no horizontal overflow on any capture.

## NOT VERIFIED (no capability in this session)
- Mobile viewports (no 390px pass this round).
- Real-browser video autoplay, marsa preloader dismissal, mailto/PDF-download click-through.
- Whether `thumbnail` fields are consumed anywhere (ProjectsPage out of scope).

## Evidence
Screenshots: `AppData/Local/hermes/cache/scratch/audit/` (34 PNGs: home/about/process/resume/
projects/cat-*/det-*). Pixel stats reproduced with PIL in-session. Lane D script:
`cache/scratch/lane-d-check.py`. Prior commissioned lanes (deleg_3a610f3b) stalled with zero
output and were taken over directly; no source files were modified by this audit.
